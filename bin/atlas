#!/usr/bin/env php
<?php

use Atlas\Application;

require __DIR__.'/../vendor/autoload.php';
$container = require __DIR__.'/../config/container.php';

$app = Application::create(
    $container
);


$app->setDefaultCommand('chat');

$app->run();